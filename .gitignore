# Build output
/build/
/build-*/
/out/
/cmake-build-*/
*.o
*.a
*.so
*.so.*
*.dylib
*.dll
*.exe

# Compile artifacts
compile_commands.json
.cache/
.ccls-cache/

# Meson subprojects (lives in dep/): submodules and overlays are tracked;
# the wrapdb fetches (libpng-*, tiff-*) and meson's transient cache are not.
/dep/libpng-*/
/dep/tiff-*/
/dep/packagecache/
/dep/.wraplock

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Local dev
.env
.env.local

# ImGui's runtime layout file
imgui.ini
